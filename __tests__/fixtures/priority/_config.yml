theme: fake
meta_generator: false

permalink: :title
pretty_urls:
  trailing_index: false # no trailing 'index.html'
  trailing_html: false # no trailing '.html'

csp:
  enabled: true
  priority: 200

  policies:
    - pattern: ^.$$
      prod:
        directives:
          img-src:
            - https://the-only-source.com
